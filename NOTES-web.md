# DIVA-01 Web —— NOTES（网页版 v1：能弹 / 能听 / 能导出）

- 日期：2026-09-18 · 产物目录 `D:\OpenClawTemp\diva-web\`（index.html + assets，纯前端零依赖）
- 来源：忠实移植 Max 版 `diva-02.maxpat` 的**音频核心**（生成器 `build_diva02.js`）与**面板**（`stepA/roll.js`）
- 参考 A/B 靶子：`diva-synth/repro/diva-repro-line1.mp3`（Max 版导出的同一句「世界で一番お姫様」）
- 自测：两轮全过（轮1 无头浏览器端到端 + 导出非静音；轮2 与 Max 版参考渲染 A/B）

---

## 0. 一句话

把 Max 里的单声部元音引擎**用 Web Audio 手写复刻**，配上 Canvas 自绘的同一张面板，做成一个
「打开就能弹、戴耳机能听、点一下能导出 WAV」的网页；部署在 GitHub Pages 上，手机也能开。

---

## 1. 逐模块讲解（按信号链顺序，含大白话）

### 1.1 整体数据流

```
鼠标 → 面板(Canvas 2D 自绘) → 事件 → 播放调度器(ms 时间线)
                                      │
                          ┌───────────┴────────────┐
                    实时播放                    导出
              DivaEngine(在线 AudioContext)   DivaEngine.render(OfflineAudioContext)
                          └───────────┬────────────┘
                                同一套节点图
                                      ↓
                          音源 → 元音门 → 共振峰组 → 包络 → 毛刺 → 音量 → 削波 → 输出
```

**为什么这样接**：实时和导出走**同一个** `DivaEngine`。导出不是"录音"，而是把同样的节点图放进
离线上下文里跑一遍——所以"听到的"和"导出的"天然一致（Max 版也是这个思路：导出走同一条 recdir/rec 通路）。

**大白话**：同一个乐器，一边对着麦克风听，一边拿录音笔录，谱子、手法完全一样。

### 1.2 音源（saw + train → tilt → 存在感 → 气声）

| 环节 | 参数 | 作用 |
| --- | --- | --- |
| `saw~` | ×0.2 | 锯齿波，1/n 谐波列，是"音高本体" |
| `train~` | 2.2727ms / 0.03 → 1.32% 占空比，×1.0 | 窄脉冲串（声门脉冲），频谱近乎平坦 → "嗡嗡的芯" |
| 二者相加 | — | 脉冲给骨架，锯齿给音高感 |
| `onepole~` tilt | `250·44^bright`（默认 0.85 → 6230Hz） | 亮度旋钮：一阶低通，越往右越亮 |
| 存在感 | `press = tilt + 3.4·(tilt − onepole~700(tilt))` | 把 700Hz 以上"拎出来"再放大 3.4 倍 → 1–4kHz 的"存在感/穿透力" |
| 气声 | `noise → reson~(2800, Q1.4, g0.45)` | 呼吸声，中心 2.8kHz |
| 空气带 | `noise → reson~(5500, Q1.2, g0.35) ×0.22` | 空气感；**注意它走的是"气声电平"的增益域**（这是 Max 版原样接线，不是笔误） |

**大白话**：先做出"芯"（脉冲）和"肉"（锯齿），再用一个亮度旋钮决定暗还是亮；
"存在感"就是把中高频挑出来放大一点点，让声音在伴奏里站得住；气声就是唱歌时的"哈——"那口气。

### 1.3 元音门 + 共振峰组（这是"能唱出日语元音"的核心）

- **元音门** `VG`：辅音段还没结束时=0（把元音掐掉），辅音段结束（`durMs`）时打开（6.8ms 斜坡）。
- **5 个元音 × 4 条共振峰并联**：每个元音是 4 个 `reson~` 并联相加（F1–F4），五组之间用**交叉淡化**选一个
  （`expr($f1==i)` + `rampsmooth~ 2000/2000` ≈ 45.35ms）。

| 元音 | F1 | F2 | F3 | F4 |
| --- | --- | --- | --- | --- |
| a | 850/70 | 1200/90 | 2800/120 | 3500/150 |
| i | 300/70 | 2300/90 | 3000/120 | 3600/150 |
| u | 350/70 | 1250/90 | 2300/120 | 3200/150 |
| e | 550/70 | 2000/90 | 2600/120 | 3400/150 |
| o | 500/70 | 900/90 | 2600/120 | 3300/150 |

（写成"中心频率/带宽"，Q = 中心/带宽；四条电平配平 GTRIM = 0.9 / 0.95 / 0.9 / 0.8）

**为什么这样**：人声的元音差别**只**由共振峰位置决定（声带是同一套）。所以只要 5 套滤波器组 + 交叉淡化，
一个声源就能唱出 a i u e o。

**大白话**：喉咙给"音高"，嘴巴给"口型"。这里用 5 组"电子嘴巴"（每组 4 个滤波器）代替真嘴，
切换口型就是切换滤波器组；45ms 的交叉淡化让"阿→衣"听起来是滑过去的，不是啪一下跳。

### 1.4 包络 + 低搁架 + 音高表情

- `adsr~ 15 120 0.9 250`：15ms 起音（不爆音）、120ms 衰减到 0.9、松键 250ms 释放。
- 600Hz 低搁架：`x − 0.28·onepole600(x)` → 低频整体降约 2.85dB，**不改变共振峰位置**，只是把"轰"削掉一点。
- **颤音**：5.75Hz 正弦 × 深度（cents，默认 30）× 400ms 渐入包络 → 加到 osc.detune。
- **scoop（音头下探）**：`−24 cents` 起，元音门打开后 120ms 回到 0 —— 这是"开口那一下的滑音感"。
- **puff（气声喷）**：元音门打开瞬间给气声电平一个 15ms 起 / 180ms 落的脉冲 → "pa"的爆破感。

**大白话**：包络是"轻轻起、稳稳唱、慢慢收"；颤音是"嗓子自然的抖动"；scoop 是"起音那一瞬间音准略低再爬上来"；
puff 是"开口先喷一口气"。

### 1.5 辅音系统（18 个辅音表 CTAB）

时间语义（与引擎完全一致）：`gate↑` 后
```
0 ──(bStart)──▶ 噪声/浊音杠包络升到 1 ──(durMs)──▶ 落到 0（20.4ms 斜坡）
0 ─────────────(durMs)───────────────────▶ 元音门打开（ADSR/颤音/puff 同时触发）
```

| 类别 | 辅音 id | 做法 |
| --- | --- | --- |
| 爆破/塞音 k t p | 1 / 2 / 3 | 先静默 `bStart`（成阻）、再短噪声爆发（80/72/70ms），带通分别 2500 / 4500 / 1200Hz |
| 擦音 s sh h f | 4 / 5 / 6 / 7 | 无静默，长摩擦噪声（130/140/70/110ms），带通 6000 / 3200 / 1100 / 1200Hz |
| 鼻音 m n ん | 8 / 9 / 10 | 无噪声，走"浊音杠"（声源 → `onepole~800` → 0.45/0.50/0.52），ん 的段长 5000ms（整音都是鼻音） |
| 弹音 r | 11 | 17ms 静默 + 35ms 短带通，浊音杠 0.34 |
| 滑音辅音 y w | 12 / 13 | 无噪声，先落到元音 i / u，70ms 后滑到目标元音（拗音"きゃ"就是这么做的） |
| 浊塞音 g d b | 14 / 16 / 17 | 成阻 + 爆发 + **浊音杠**（带音高的阻塞音） |
| 浊擦音 z | 15 | 120ms 噪声 + 浊音杠 |
| 促音 っ | 18 | 110ms 纯静默（闭锁） |

**大白话**：辅音 = "先把嘴堵一下/漏点气"，再让元音出来。堵多久、漏什么气，就是 CTAB 那张表。

### 1.6 复古毛刺层 + 输出

```
汇总 ─┬────────────────────────────▶ ×(1−amt) ─┐
      └▶ 6bit 量化 ─▶ 低通7500/Q0.3 ─▶ ×0.72 ─┴▶ + ▶ 音量(dB) ▶ 硬削波±1 ▶ 输出
```
**大白话**：给声音"降bit + 降采样感"，做成老设备的颗粒感；amt 是干湿比，拖到 0 就是完全干净。

### 1.7 面板（Canvas 2D 自绘，1280×800）

- 配色/几何与 Max 版一致：底 `#0A0E13` / 卡 `#121820` / 青 `#39C5BB` / 粉 `#F2A9DA`。
- 分区：顶部传输条（▶/■/↺ + 序列信息 + NOW 当前音节）｜左侧五十音 pad（清音/浊音/半浊音/拗音 四个页签）
  ｜中央卷帘（示例 13 音 + 罗马克马标签 + 粉色播放头）｜底部「示例 / 清空 / 导出 WAV」+ 音色四参 + GLITCH。
- 交互：点 ▶ 播放示例；点五十音格试听该音节（含辅音）；点卷帘上的音符块试听该音；拖动参数卡改值。

**为什么用 Canvas 自绘而不是 HTML 控件**：与 Max 版保持同一套视觉语言（`stepA/roll.js` 就是这么画的），
也便于把 Max 面板截图与网页截图逐像素对比。

**大白话**：整块面板是一张"画出来的图"，所以颜色、字号、圆角和 Max 版一模一样。

### 1.8 导出 WAV

`OfflineAudioContext`（44.1kHz）→ 渲染 **2 遍**（+150ms 引导 +800ms 收尾）→ 16bit PCM WAV → 浏览器下载。
实测：13.722s / RMS 0.0067 / 峰值 0.0522。

**大白话**：等于在后台用"虚拟声卡"把整首歌播两遍并录下来，直接给你 wav 文件。

---

## 2. 两轮自测（留证据）

### D-1 音高曲线编辑器（2026-09-18 晚，网页版实现）

数据模型：`note.curve = [{u,semi}...]`（u=0..1 归一化位置，semi=半音）；引擎把「接缝滑音段 + 音符内弯音」
统一采样成频点排程（≈50Hz，点间指数插值 = MIDI 线性）——比 Max 版的 60Hz 消息流更精确，且导出与实时同路。

| 交互 | 实测（`tools/run_test3.js`，真实鼠标） |
| --- | --- |
| 拖音符线中段上/下 = 生成弯音（1 行 = 1 半音，步进 0.25） | ✓ 上拖 30px → `curve=[{0,0},{0.5,+2},{1,0}]`，状态行报 `+2.00 半音 → 峰值落在 118ms` |
| 拖接缝滑音手柄（左 = 变长，10ms 步进，0–600ms） | ✓ `glide 260 → 0`（向左拖到底） |
| 双击控制点 = 清除曲线 | ✓ `curve → null` |
| 带曲线播放 / 导出 | ✓ `playing=1`；导出 7.336s RMS 0.0067 峰值 0.0506（非静音） |
| console | ✓ 0 error / 0 warning |
| 辅音段底衬 + 越界提示 | ✓ 每个音头画半透明辅音段；控制点落进辅音段时状态栏提醒「滑音请拉长到 > Nms」 |

截图：`test/d1-editor.png`。

### 交互修复（2026-09-18 22:1x，v1.1）：卷帘可添加音符

反馈「添加不了音符」→ 定位：`onDown` 里没有「空白处新建」分支（此前只能听示例、编辑已有音符、播放导出）→ 已补并上线：

| 交互 | 实测（`tools/run_add.js`，真实鼠标） |
| --- | --- |
| 点卷帘空白 = 新建音符（默认 500ms、假名 `a`、时间吸附 125ms） | ✓ `+1 音 midi=75 t=4625 d=500` |
| 按住左右拖 = 调整长度（吸附，最短 100ms） | ✓ `d 500 → 750` |
| 同音高同格重复点击 = 挡住并提示 | ✓ 『该位置已有同音高音符』 |
| 清空后仍可添加（total=0 → 自动给 8s 画布） | ✓ `1 音 / total=8000` |
| console | ✓ 0 error / 0 warning |

`tools/run_audit.js` 增加第 10 节覆盖：清空 → 空白添加 → 1 音 → 示例恢复；**24/24 全过**。
证据：`test/add-note.png`、`test/add-report.txt`。

- 备注：吸附是**时间域 125ms**（标尺半格），不是 BPM 网格——网页版无 tempo 概念。
- 待办候选：选中音符 → 点五十音格「填词」（Max 版已有此交互；网页版五十音格当前=仅试听）。

### 轮 1 · 程序化端到端（无头 Edge + playwright-core，`tools/run_test1.js`）

真实鼠标事件打在**屏幕上的页面**上（不是调内部函数）：

| 步骤 | 判据 | 结果 |
| --- | --- | --- |
| 打开页面 | `window.__diva.version` / 示例 13 音 / 6386ms | ✓ |
| 画布绘制 | 抽样像素 92.7% 非背景；青 177 点、粉 29 点 | ✓ |
| 点五十音格「か」 | 状态行变"试听 か（ka）cons=1 vowel=0" | ✓ 真实点击命中 |
| 点 ▶ | `playing=1, cur=3`（第 4 音），6.39s 后 `playing=0` 播完 | ✓ |
| 点「导出 WAV」 | 触发真实下载 `DIVA01_web_*.wav`（2,420,608 B） | ✓ |
| 导出内容 | 13.722s / **RMS 0.0067 / 峰值 0.0522（非静音）** | ✓ |
| 拖参数 | BREATH 0.28 → 0.8 | ✓ |
| **console** | **errors 0 / warnings 0 / pageerror 0 / requestfailed 0** | ✓ |

截图：`test/shot-1-initial.png`、`shot-2-kana-click.png`、`shot-3-playing.png`（含粉色播放头+高亮音符）、`shot-4-after-export.png`。

### 轮 2 · 与 Max 版参考渲染 A/B（`tools/run_test2.js` + `tools/analyze.js` + `tools/ab_bench.js`）

**同一句「世界で一番お姫様」**（`repro/diva-repro-line1.mp3` vs 网页版单遍渲染，**出厂 BRIGHT=0.55 / 电平标定 CAL=0.97**）：

| 指标 | Max 版参考 | 网页版 | 差 |
| --- | --- | --- | --- |
| 时长 | 7.268 s | 7.336 s | **+0.9%** |
| RMS | 0.00665 | 0.00659 | **−0.08 dB** |
| 峰值 | 0.04599 | 0.04858 | +0.5 dB |
| 20–200 Hz 占比 | 0.01% | 0.02% | 一致 |
| 200–1000 Hz 占比 | 56.5% | 58.4% | +1.9pp |
| 1–4 kHz 占比 | 40.3% | 39.6% | **−0.7pp** |
| 4–8 kHz 占比 | 2.99% | 1.91% | −1.1pp |
| 谱重心 | 1390 Hz | 1349 Hz | **−41 Hz（−3%）** |

**"出厂 BRIGHT"的来历**：网页版源频谱（Web Audio 周期波表 vs Max `train~`）比 Max 版亮一档，同参数（0.85）下谱重心 1953 Hz（偏差 +563 Hz）。刷新引擎后重扫（`tools/sweep_bright.js`）：BRIGHT=**0.55** 时三项占比与参考几乎重合（57.5/40.5/2.0 vs 56.5/40.3/3.0）→ **定为出厂值**，并把电平标定 CAL 同步校到 0.97（0.55 下响度与参考齐平）。想回 Max 原值：把 BRIGHT 拖回 0.85（会偏亮一档）。

**稳态元音测试台**（Max 自己的 15 音台架 D4/A4/C5 × a i u e o，`tools/ab_bench.js`）：
逐音对的谱重心 **Max 平均 1353 Hz vs 网页版 2141 Hz**；稳态元音电平低 5–8 dB（短语整体电平已对齐）。

**听感样本**：`test/AB-web-vs-max-line1.mp3` = Max 版 → 0.6s 静音 → 网页版。

---

## 3. 已知差异（诚实清单：网页版 vs Max 版）

1. **中频偏亮 —— 已按实测校准（出厂 BRIGHT 0.55）**：网页版源频谱比 Max 版亮一档（等效 BRIGHT −0.3）；
   校准后谱重心 1349 vs 参考 1390 Hz，三项占比全部进 2pp 内。把 BRIGHT 拖回 0.85 就会重现偏亮（这是 Max 场景原值）。
2. **稳态元音电平低 5–8 dB**：短语整体响度已用电平标定 `C.cal` 对齐，但纯元音段仍偏小（辅音相对偏多）。
3. **>8 kHz 能量偏低**：0.30% vs 0.16%（参考反而更低，方向一致但量级不同）；4–8 kHz 已在同档。
4. **信号原语不可能比特级一致**：Max 的 `train~`（窄脉冲串）与 Web Audio 周期波表、`reson~` 与 `IIRFilterNode`
   在数值精度、过零相位、内部限幅上都不同 → 相位/瞬态细节必然有差（不影响"能弹能听"）。
5. **导出为 2 遍循环**（任务要求）；Max 版自由时序导出是 1 遍 + 800ms 收尾。
6. **没有的东西**：BPM/量化网格编辑、录音、键盘弹录、MIDI 导入、Max 原生控件（kslider/dial）——v1 范围外；
   浏览器无文件系统，故「打开文件夹」改为状态行显示文件名。
7. **面板是简化版**：保留了传输条/五十音/卷帘/按钮行/音色四参，未做 Max 版 Step D 的仪表环/LED 点阵/发光全套皮。
8. **拗音仍是滑音近似**（与 Max 版一期一致）。

---

## 4. 本轮踩的坑（长期有效）

0. **GitHub Pages 的 favicon 404**：`file://` 本地测没有 favicon 请求，一上 Pages 就冒 2 条 404（浏览器自动抓 favicon.ico）→ 内联 data-URI favicon 解决。
1. **`OfflineAudioContext` 渲染整段静音**：`out` 节点忘了 `connect(ctx.destination)`。
   → 教训：离线渲染"跑完没报错、长度为 0 之外全是 0"时，先查**最后一根线**。
2. **"无前置事件的 linearRampToValueAtTime"起点不确定**：Chrome 下不按预期从当前值渐变，
   表现同样是整段静音。→ 所有被自动化的参数在 `t=0` 先 `setValueAtTime(初值, 0)` 铺一个事件；
   后续用 `cancelAndHoldAtTime` 续接。
3. **`root.DIVA_TABLES = {...}` 覆盖掉了 tables.js 挂的表**（`parseSeq is not a function`）。
   → 多文件共享命名空间一律 `Object.assign(existing || {}, …)`。
4. **存在感支路漏掉"原始 tilt"**：`press = tilt + 3.4·hp` 里只连了 `3.4·hp`，声音立刻变薄变亮
   （A/B 的 200–1000Hz 占比 56%→43%）。→ 任何"加法分支"落地后要数一遍**进线数量**。
5. **一阶低通系数公式用错**：`onepole~` 是 `a0 = sin(fc·π/nyquist)`，不是 `1−exp(−2πfc/fs)`；
   6 kHz 处 0.776 vs 0.588 → 源音整体偏暗一大块。**官方参考页是第一手依据，别凭印象写 DSP。**
6. **`reson~` 用错模型**：它官方定义是 `y[n] = a0(x[n] − r·x[n−2]) + b1·y[n−1] + b2·y[n−2]`（**二极点二零点**），
   `r = exp(−π·bw/fs)`，`a0 = gain·(1−r)`。我先写成"纯二极点"→ 带宽宽一倍、低频泄漏（20–200Hz 从 0.01% 涨到 0.4%）。
   → 数值验证三件套：**峰值 = gain、−3dB 带宽 = cf/Q、DC/Nyquist 深度抑制**，三条都过才算移植对。
7. **`Get-Content -Raw | Set-Content -Encoding UTF8` 会把 UTF-8 源码写坏**（PS5.1 按 GBK 解码 →
   GBK 双字节"吃掉"后面的 ASCII 字节，连换行都可能被吞）。本项目 `engine.js` 中招后**不可逆**，
   只能重写。→ **源码/文档一律用 read/write/edit 工具改；绝不用 PowerShell 做文本替换**（TOOLS.md 早有记录，本人又踩一次）。
8. **`.NET` 调用不吃 PowerShell 的 `Set-Location`**：`[IO.File]::ReadAllBytes('assets/x')` 会跑到别处 →
   一律绝对路径。
9. **抽头录音不可全信**：`work/rec/d02c_tap2_off.wav` 与 `tap1` 逐谐波比值恒为 −6.0 dB（＝×0.5），
   明显不是"共振峰后"信号 → 用抽头做结论前先验一次"这条抽头是不是名副其实"。
10. **浏览器自动播放策略**：无头测试要带 `--autoplay-policy=no-user-gesture-required`，
    否则 `AudioContext` 起不来（本项目的导出走 OfflineAudioContext，不受影响）。
11. **`.js` 里的假名依赖解码**：GitHub Pages 对 `.js` 会带 `charset=utf-8`，本地 `file://` 也按文档编码走；
    但仍要在截图里**肉眼确认假名不是豆腐块**（本轮已确认）。

---

## 5. 部署（GitHub Pages）

- 仓库：https://github.com/chenchi-arch/diva-web （public，main 分支根目录）
- 站点：**https://chenchi-arch.github.io/diva-web/**
- 线上冒烟（2026-09-18 21:0x，无头 Edge 打线上 URL）：`__diva.version=web-v1`、示例 13 音/6386ms 装载 ✓；真实点击 ▶ → `playing=1, cur=3` 逐音推进 ✓；index.html 与 5 个 assets 全部 200 ✓
- 首次冒烟抓到 2 个 404（浏览器自动请求 favicon.ico）→ 已内联 SVG data-URI favicon 修复，重推后 console 0 error/warning
- 网络备注：本机直连 github.com 不稳（DNS 首个 IP 超时）；`git push` 经系统代理 127.0.0.1:7897 间歇可用，用重试循环推（第 6 次成功）；`api.github.com` 直连正常。
- **2026-09-18 22:3x · v1.1（卷帘添加音符）**：`api_push.js` 推送 commit `f8f40c4` → 线上校验：`assets/app.js` 200、含 marker `web-v1.1` ✓（`tools/httpcheck.js`）。

## 6. 复现命令

```powershell
cd D:\OpenClawTemp\diva-web
node tools/run_test1.js                 # 轮1：无头端到端 + 导出 WAV
node tools/run_test2.js                 # 轮2：渲染单遍/两遍 WAV 到 test/
node tools/analyze.js test\ref-line1-mono.wav test\web-line1.wav    # 时长/电平/频段占比
node tools/ab_bench.js <max台架.wav> test\bench-web-glitch0.wav      # 稳态元音逐音对比
node tools/sweep_bright.js              # 亮度扫描（定位"偏亮"的等效 BRIGHT）
```
